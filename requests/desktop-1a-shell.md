Add desktop mode's switch and shell per `specs/desktop-mode-spec.md` §1 and §2 (read the spec's "Decisions" table first — do not re-open those): a tiny store `web/src/lib/desktop.ts` copied from the `web/src/lib/haptics.ts` shape (localStorage key `collie:desktop:v1` → `{ on: boolean, typing: "composer" | "direct" }`, defaults `{ on: false, typing: "composer" }`, lazy-loaded on first read, `useSyncExternalStore`, `setDesktop(on)`, `setTyping(t)`, `__resetDesktop()`, `useDesktop()` → `{ on, typing }`); a Settings `<Card>` row (same pattern as `haptics-control.tsx`: monitor icon, "Desktop mode", "Sidebar layout and direct keyboard typing. For a computer, not a phone.", `<Switch>`) that when `on` shows two more rows — "Typing surface" (Composer / Direct, writes `typing`) and "Idle pause" (read-only text: "2 h on desktop. Pauses polling; it does not lock the shell — use your OS screen lock.") — and hides the haptics card; a `DesktopShell` rendered by `web/src/routes/root.tsx` when `on` (two-column grid, `aside` fixed 280 px + `main` with `<Outlet/>`, banners above both, `BottomNav` not rendered) with an always-visible, never-collapsible one-line off-ramp strip above sidebar and pane reading "Desktop mode is on · Turn off" (click turns it off); a `web/src/components/desktop-sidebar.tsx` with the session switcher at top, the unchanged `<SpaceTree>` (current pane row highlighted), and a nav list Spaces / Traces / Files / Settings at the bottom using the same gating props `BottomNav` uses. When `on`, the pane, history and files routes drop `max-w-screen-sm`, and the index route shows an empty state "Pick a pane". Components branch on `useDesktop().on`; no media query decides which controls exist.

Where: new `web/src/lib/desktop.ts` (+ test), new `web/src/components/desktop-shell.tsx` and `web/src/components/desktop-sidebar.tsx` (+ tests), `web/src/routes/root.tsx`, the settings route and its cards next to `web/src/components/haptics-control.tsx`, the pane / history / files / index routes' width classes; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means:
- Toggle on in Settings → sidebar + pane layout with the off-ramp strip, no bottom nav; toggle off (or click "Turn off") → the tree is byte-for-byte what renders today.
- New tests with `on: false` assert: `BottomNav` is in the tree; `DesktopShell`, the sidebar and the strip are not. A snapshot test pins `lib/desktop.ts` defaults to `{ on: false, typing: "composer" }`.
- `bun run test` at root still reports 859 tests across 37 files and `cd web && bun run test` still reports 2681 passed + 19 todo with **no existing test file edited**; new tests only add to the web count. `bun run typecheck` is clean.
- MINOR bump to **0.56.0** in `herdr-plugin.toml`, `package.json`, `web/package.json`, with one `CHANGELOG.md` `### Added` line: "Desktop mode (Settings toggle): sidebar + pane layout".

Out of scope: `web/src/hooks/use-display-prefs.ts` (spec says untouched); `bridge/`; `web/src/hooks/use-direct-typing.ts`, `web/src/components/composer.tsx`, `web/src/hooks/use-long-press.ts` (phases 2–3); idle-lock duration, `document.title`, hotkeys, hiding the Keys / Type / attach / swipe / `PaneStrip` / haptics / tapToFocus controls inside the pane view (chunk 1b); any resizable or collapsible sidebar; auto-detect by viewport.
