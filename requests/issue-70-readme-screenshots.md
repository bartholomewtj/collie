Fix #70: replace assets/dashboard.png and assets/space-detail.png. They still show the Herd dashboard and the /space/:spaceId screen that 0.41.0 deleted. Reshoot against the current Spaces tree. Worth one shot of a space expanded to its tabs with a blocked space auto-expanded at the top — tree and triage-on-rows in the same frame. Update the README.md Demo table captions so they match the new images, not the deleted screens.
Where: assets/dashboard.png, assets/space-detail.png, README.md (Demo table around the dashboard/space-detail figures). Capture from the live bridge on loopback (127.0.0.1:8787) at a phone viewport; use the operator's existing COLLIE_TRUSTED_USER header. Do not invent screenshots.
Done means: both PNGs show the current tree (not the deleted Herd or space-detail screens); README captions describe the tree; `gh issue close 70` is valid after this lands. Doc-only plus assets — no version bump.
Out of scope: other screenshots (ask-question.png, keys.png, session-switcher.png, settings.png, collie-hero.webp), bumping Collie, editing web/src, capturing private pane text into the images, rewriting the rest of the README.
