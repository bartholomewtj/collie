Cut Collie 0.44.0 for merged #77 (idle panes hide the live TUI). Bump the three version files from 0.43.2 to 0.44.0 and add the CHANGELOG entry. Do not tag — the engineer tags after merge.
Where: herdr-plugin.toml, package.json, web/package.json, CHANGELOG.md. Run scripts/check-version.sh — it must print ✓.
Done means: all three version files say 0.44.0; CHANGELOG newest heading is `## [0.44.0] - 2026-08-20` with this Added line from the #77 PR body, citing 08db5c3: "Idle panes hide the live terminal tail when the transcript already has the newest turn, so the thread reads as chat instead of a TUI snapshot. Show live still peels it back. (08db5c3)". Keep the 0.43.2 heading intact under it. check-version.sh prints ✓.
Out of scope: tagging, pushing to main, restarting the bridge, editing adws/, quality.py, screenshots, web/src (already merged in #77), scripts/check-doc-links.test.ts (already fixed on this branch), other issues. Do not rewrite the 0.43.1 or 0.43.2 CHANGELOG headings.
