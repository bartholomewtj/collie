Add desktop mode's drag-and-drop image upload per `specs/desktop-mode-spec.md` §4 / §10 phase 3 (read the spec's "Decisions" table first — do not re-open those). When `useDesktop().on` and a pane is open: a window-level `dragover` and `drop` listener calls `preventDefault()` so a stray drop never navigates the page; a drop of files only (ignore text/URL drops) takes the first image file and hands it to the same upload path `onPasteImage` uses in `web/src/components/composer.tsx`; the returned host path shows in the direct-typing strip as the existing one-click "Type path" chip and is never typed unprompted; a non-image file drop shows a status "Only images can be dropped" and sends nothing. With `on: false` nothing is registered and a drop behaves as today.

Where: `web/src/components/composer.tsx` (reuse its image upload path; verify the symbol name), the file that defines `DirectTypingStrip` and the paste-hold / "Type path" chip store (verify paths — `web/src/lib/paste-hold.ts` and `web/src/components/direct-typing-strip*.tsx`), a new `web/src/hooks/use-drop-upload.ts` (+ test), `web/src/components/agent-chat.tsx` to mount it; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: tests cover a file drop while `on: true` is `defaultPrevented` on both `dragover` and `drop`, uploads the image and shows the "Type path" chip without sending keys; a text drop does nothing; a non-image file shows the status and uploads nothing; with `on: false` no listener is attached and `drop` is not `defaultPrevented`. `cd web && bun run test` passes with no existing test file edited; root `bun run test` still 859; `bun run typecheck` clean; PATCH bump to **0.56.11** in `herdr-plugin.toml`, `package.json`, `web/package.json` with one `CHANGELOG.md` `### Added` line under `## [0.56.11]`: "Desktop mode: drag an image onto the pane to upload it; the host path appears as a Type path chip".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; `web/src/components/pane-actions-sheet.tsx`.
