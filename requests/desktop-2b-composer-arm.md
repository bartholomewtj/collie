Add desktop mode's composer behaviour and direct-typing arm/release per `specs/desktop-mode-spec.md` §3 ("Composer surface", and "Arm" / "Armed = focused" under "Direct surface") and §6 (the Ctrl+` row only) — read the spec's "Decisions" table first and do not re-open those. Gated on `useDesktop().on`: (1) in the composer, Enter sends and Shift+Enter inserts a newline, only when `!e.nativeEvent.isComposing`; Ctrl+Enter still sends; Esc, Tab and arrows pass through to the pane when the box is empty; Ctrl+C never passes through from the composer; Send keeps the `isDestructiveInput` two-tap confirm. (2) When `typing === "direct"`, the existing composer textarea and `useDirectTyping` are reused — no second textarea or key queue; `DirectTypingStrip` ("Typing into terminal — Stop") renders where the input row was and the input is visually hidden behind it but keeps focus. (3) Arm by clicking the mirror (`focusFromMirror` → `composerRef.focusInput()` + `direct.activate()`) or Ctrl+`; arming is refused if the click ends with a non-collapsed `window.getSelection()`; the `replyDraft().length > 0` refusal is bypassed only inside the `on` branch of `activate()`, not removed. (4) Armed = focused: the mirror ring and the strip follow the textarea's own focus/blur; release on the Stop button, Ctrl+`, any `pointerdown` outside the mirror (sidebar, header, find bar, a prompt button, a popover) and `window.blur`; the idle-pause cover blurs it. No Esc-Esc. (5) Ctrl+` is a global chord in `useDesktopHotkeys` that arms / releases and switches `typing` to `direct` if it was `composer`. (6) `readOnly` device or `gone` pane: strip greyed with the reason, no capture. (7) Update the armed-state comment in the hook header and the matching `CLAUDE.md` paragraph to say the surface pref is stored but armed state is not.

Where: `web/src/components/composer.tsx`, `web/src/hooks/use-direct-typing.ts`, `web/src/components/agent-chat.tsx`, `web/src/hooks/use-desktop-hotkeys.ts`, the file that defines `DirectTypingStrip` (verify its path), `CLAUDE.md`, new tests beside each touched file; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: tests with `on: false` show Enter still inserts a newline and arming with a non-empty `replyDraft` still refuses with the existing status message; tests with `on: true` cover Enter-sends, Shift+Enter newline, Enter while composing does nothing, empty-box Esc/Tab/arrow pass-through, Ctrl+C not passed, mirror click arms, selection click refuses, Ctrl+` toggles and flips `typing`, outside `pointerdown` and `window.blur` release, idle cover blurs, readOnly/gone greyed. `cd web && bun run test` passes with no existing test file edited; root still 859; typecheck clean; PATCH bump to **0.56.4** with one `CHANGELOG.md` `### Added` line under `## [0.56.4]`: "Desktop mode: Enter sends, click-to-type into the terminal, Ctrl+` to arm/release".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; `web/src/lib/key-queue.ts`; the keydown mapper, modifier chords, paste handling and Ctrl+F (next chunks).
