Fix four gaps in commit 1dae038 ("Add desktop mode switch and shell layout") against `requests/desktop-1a-shell.md` and `specs/desktop-mode-spec.md` §2 and §9. (1) `web/src/routes/files.tsx` removed `max-w-screen-sm` for everyone — restore it and drop it only when `useDesktop().on`. (2) Apply the same conditional width drop to the pane route (`web/src/routes/agent-chat.tsx`) and the history route; when `on` is false the class list is exactly what it was at 9003293. (3) `web/src/components/desktop-sidebar.tsx` passes `onNewSpace={() => {}}` and `onNewTab={() => {}}` — wire them to the real `useSpaceActions()` handlers the way `web/src/routes/tree.tsx` does, including the new-space dialog. (4) Add the missing tests: with `on: false`, rendering `RootLayout` contains `BottomNav` and does not contain `DesktopShell`, the sidebar or the "Desktop mode is on" strip; with `on: true` the inverse holds, "Turn off" flips `on` back to false, and the sidebar nav shows Traces / Files only under the same conditions `BottomNav` shows them.

Where: `web/src/routes/files.tsx`, `web/src/routes/agent-chat.tsx`, the history route under `web/src/routes/`, `web/src/components/desktop-sidebar.tsx`, new tests beside `web/src/components/desktop-shell.tsx`, `web/src/components/desktop-sidebar.tsx` and `web/src/routes/root.tsx`.

Done means: `cd web && bun run test` passes with no existing test file edited and the new tests added; `bun run test` at root still reports 859 tests; `bun run typecheck` clean; version stays 0.56.0 with no CHANGELOG change.

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-direct-typing.ts`, `web/src/components/composer.tsx`, `web/src/hooks/use-long-press.ts`; everything listed in `requests/desktop-1b-phone-bits.md` (idle 2 h, `document.title`, hotkeys, hiding pane-view controls).
