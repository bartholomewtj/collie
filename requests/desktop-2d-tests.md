Close the reviewer's remaining test findings on commit 0260861 (`adws/adw_data/sessions/1b24cf9e/context_handoff/review.md`, "Blocking" section) by adding cases to four test files that already exist on this branch — product code is approved and does not change. (1) `web/src/hooks/use-direct-typing-paste.test.tsx`: pasting `echo hi\n` while captured holds with `lines: 1` and the `onSend` sequence contains no `"Enter"`; pasting `rm -rf /tmp/x` holds with a `reason` matching `/rm -r/`; an image paste calls `onSend` with `textToKeySequence("/host/shot.png")` only after the chip is clicked; with `on: false` a paste leaves the hold `null` and `sendKeys` uncalled. (2) `web/src/components/direct-typing-strip-paste.test.tsx`: `pointerdown` on the Send button is `defaultPrevented` (focus stays in the textarea). (3) `web/src/lib/find-request.test.ts`: `requestFindOpen()` with no subscribers does not throw. (4) `web/src/lib/paste-hold.test.ts`: `__resetPasteHold()` returns the store to `null`. Do not edit any test file that existed before 0260861.

Where: `web/src/hooks/use-direct-typing-paste.test.tsx`, `web/src/components/direct-typing-strip-paste.test.tsx`, `web/src/lib/find-request.test.ts`, `web/src/lib/paste-hold.test.ts`; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: every case above exists and `cd web && bun run test` passes; `bun run test` at root still reports 859; `bun run typecheck` clean; PATCH bump to **0.56.8** in `herdr-plugin.toml`, `package.json`, `web/package.json` with a `CHANGELOG.md` `### Changed` line under a new `## [0.56.8]` heading: "Desktop mode: test coverage for paste hold and find request edge cases".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; any non-test file (product code is approved and does not change).
