Fix issue #1: remove the loopback-Origin bypass from the same-origin gate so any `localhost`/`127.0.0.1`/`[::1]` Origin no longer passes when it does not match the request Host. Require `originHost === host` or an entry in `COLLIE_ALLOWED_ORIGINS`. Defence in depth: reject JSON write routes whose `Content-Type` is not `application/json`, so a cross-site POST forces a CORS preflight.
Where: bridge/server.ts (the `LOOPBACK_HOST` regex near line 91 and the `allowed` check near lines 1141-1145; `req.json()` handling near line 626), bridge/server.test.ts (the test at lines 92-99 that currently asserts the bypass).
Done means: a request with `Origin: http://127.0.0.1:8787` and a non-matching `Host` is rejected; the test at server.test.ts:92-99 is rewritten to assert rejection; a new test covers a same-origin request still passing and a `text/plain` POST to a JSON route being rejected; the touched tests pass under `bun test ./bridge`.
Out of scope: the other audit issues (#2-#12), reporting upstream, changes to the device gate or COLLIE_TRUSTED_USER, any frontend change beyond what the tests need. Note: 39 pre-existing tests fail on Windows for POSIX path reasons — judge by the tests touched, not the whole suite.
