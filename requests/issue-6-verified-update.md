Fix issue #6: pin `collie-ctl.sh`'s Herdr-managed checkout update to the newest `vX.Y.Z` release tag on the remote instead of checking out unverified origin HEAD, refuse with an actionable error if no release tag exists unless overridden by `COLLIE_UPDATE_REF`, freeze both dependency lockfiles during build with `--frozen-lockfile`, and mirror these changes in `contrib/windows/collie-ctl.ps1`.
Where: scripts/collie-ctl.sh (lines 243-244, 650-680), scripts/collie-ctl.test.sh (lines 843-904, 1227-1230), contrib/windows/collie-ctl.ps1 (lines 174, 184, 423-446), contrib/windows/collie-ctl.test.ps1 (lines 207-227), .adr/0011-update-pins-to-the-newest-release-tag.md, .adr/0006-update-advances-the-checkout-herdr-installed.md (lines 27-29), .adr/README.md (lines 63-75), README.md (lines 405, 433, 458-467, 487-498), CLAUDE.md (lines 85-90).
Done means: `newest_release_tag()` queries `git ls-remote` for `v*` tags, filters to strict semver matching `bridge/update.ts` (`^v[0-9]+\.[0-9]+\.[0-9]+$`), and sorts newest-first; `update_checkout` pins managed checkouts to that tag (or `COLLIE_UPDATE_REF`) and asserts the tag via `git describe --tags --exact-match`; empty tag discovery refuses update with non-zero exit and leaves HEAD unchanged; linked clones preserve `git pull --ff-only` on their branch without detaching; `cmd_build` invokes `bun install --frozen-lockfile` in root and `web/`; PowerShell mirror matches; ADR 0011 documents the decision and why signature verification is deferred; all new and updated lifecycle tests pass.
Out of scope: `bridge/update.ts` is unchanged (it already matches semver tags); the `COLLIE_UPDATE_REPO` / `origin` split is preserved (code updates from the installed origin remote while bridge queries GitHub releases API); signature verification via `git verify-tag` is deferred to ADR 0011; audit issues (#1-#4, #5, #7-#12); changing Herdr install/refresh mechanics or manifest format.
