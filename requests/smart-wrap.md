Make word wrapping clean on Collie mobile for grok, agy, and claude: keep wrapping prose, but do not wrap table, box-drawing, or full-width-highlight rows (extend the existing `noWrap` border clip), and strip trailing padded spaces so coloured empty lines go away. Same wrap toggle, same agents.
Where: web/src/lib/blocks.ts, web/src/lib/blocks.test.ts, web/src/components/ansi-output.tsx, web/src/components/ansi-output.test.tsx
Done means: wrap stays on by default; prose still wraps; table/box/full-width-highlight rows stay one visual row (clipped like the existing long `─` / `╭─╮` borders); trailing padded spaces do not wrap into extra coloured lines; wrap-off still pans column-faithful; tests cover the new noWrap cases and the trailing-space strip. Grok, agy, and claude all go through the same classifier.
Out of scope: resizing the Herdr pane, parsing live ANSI as markdown, conversation history, changing the wrap default or adding a new toggle, a per-agent wrap setting, a terminal emulator.
