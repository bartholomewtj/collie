Add desktop mode's right-click popover per `specs/desktop-mode-spec.md` §4 "Mouse (phase 3)" (read the spec's "Decisions" table first — do not re-open those). (1) `useLongPress` gains a `disabled` option that skips only the pointerdown hold timer — `onContextMenu` still `preventDefault`s and fires — and passes `{ x, y }` from the triggering event to `onLongPress`; callers pass `disabled: on` from `useDesktop().on` and nothing else, never pointer type or viewport. (2) When `useDesktop().on`, `PaneActionsSheet`, `TabActionsSheet` and `SpaceActionsSheet` render their same rows from `action-sheet-rows.tsx` inside a popover anchored at `{ x, y }`; the anchor falls back to the row's `getBoundingClientRect()` when `x`/`y` are 0 (keyboard Menu key, Shift+F10); not a new component family — same body, different container. Rename stays inline via the existing `RenameView` (Enter saves, Esc cancels); close stays two-step. Escape and any `pointerdown` outside close the popover. (3) Record the desktop exception to the "no second gesture / no second sheet" rule in `CLAUDE.md`. No hover `…` button.

Where: `web/src/hooks/use-long-press.ts` (+ its test), `web/src/components/pane-actions-sheet.tsx`, `web/src/components/tab-actions-sheet.tsx`, `web/src/components/space-actions-sheet.tsx`, `web/src/components/action-sheet-rows.tsx`, a new popover container under `web/src/components/ui/` (+ test), `CLAUDE.md`; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: tests cover `useLongPress` with `disabled: true` (no sheet on a held pointerdown; `contextmenu` still fires `onLongPress` with `{x,y}` and is `defaultPrevented`) and with `disabled: false` (existing Android long-press and `contextmenu` behaviour unchanged, all existing `use-long-press` tests pass unedited); each of the three sheets with `on: true` renders its rows in the popover at the pointer and with `on: false` still renders the bottom sheet; Escape and outside `pointerdown` close the popover; `{x:0,y:0}` anchors to the row rect. `cd web && bun run test` passes with no existing test file edited; root `bun run test` still 859; `bun run typecheck` clean; PATCH bump to **0.56.10** in `herdr-plugin.toml`, `package.json`, `web/package.json` with one `CHANGELOG.md` `### Added` line under `## [0.56.10]`: "Desktop mode: right-click opens the pane, tab and space actions in a popover at the pointer".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/components/composer.tsx`; `web/src/hooks/use-direct-typing.ts`.
