Land the ADW quality-block split that is already written in the working tree at adws/adw_modules/quality.py. That path is in protected_files — do not edit it. Write a spec describing the split, and document the change. No version bump: this is factory code, not app code.
Where: specs/ (new spec), app_docs/ (write-up). adws/adw_modules/quality.py is already implemented and must be left untouched by agents. CHANGELOG 0.43.1 already describes the first wiring — do not rewrite that heading.
Done means: a spec under specs/ describes these blocks: run_tests() = typecheck_bridge, typecheck_web, test_bridge, test_web; run_quality() = versions, version_bump, those four, test_ctl, and build. test_bridge is `bun test ./bridge ./scripts` (not root `bun run test`). test_web is `bun run --cwd web vitest run`. test_ctl is powershell on win32 else bash. build is in run_quality() only because it rewrites live web/dist. Each failing check keeps its own 4k tail. bun --cwd goes after the subcommand. app_docs/ has the write-up. Version files stay 0.43.1 (or whatever they are on this branch).
Out of scope: editing adws/adw_modules/, adws/adw_sssf_config/, adws/adw_*.py, bumping Collie, tagging, rewriting CHANGELOG 0.43.1, running bun run build against the live checkout as a "fix".
