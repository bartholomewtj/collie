Close the reviewer's three open findings on commit 60aeb58 (`adws/adw_data/sessions/9251a58e/context_handoff/review.md`, findings 15, 16 and 17) by finishing assertions in three new test files — product code is approved and does not change. Copy the `replyHandler`, `StatusSentinel` and `awaitTerminalStall` helpers from `web/src/components/composer.test.tsx` into each file that needs them; do not export them from it and do not edit any existing test file. (1) `web/src/components/composer-desktop.test.tsx`: with `on: false`, Enter inserts a newline into the textarea (value gains `"\n"`), sends nothing through `replyHandler`, and the event is not `defaultPrevented`; with `on: true`, Enter sends the typed text through `replyHandler` / `recordReply`, the event is `defaultPrevented`, Shift+Enter inserts a newline and sends nothing, and Enter while `isComposing` does nothing. (2) `web/src/components/agent-chat-arm.test.tsx`: on a `gone` pane with `on: true`, assert the strip shows the gone reason, there is no Stop button, and clicking the mirror does not arm (`active` stays false). (3) `web/src/hooks/use-direct-typing-desktop.test.tsx`: replace the dead `<output data-testid="status" />` with a real `StatusSentinel` and assert the phone refusal shows the existing status text exactly: "Send or clear the draft before typing into the terminal."

Where: `web/src/components/composer-desktop.test.tsx`, `web/src/components/agent-chat-arm.test.tsx`, `web/src/hooks/use-direct-typing-desktop.test.tsx`; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: every assertion above exists and `cd web && bun run test` passes with every file that existed before 60aeb58 unedited; `bun run test` at root still reports 859; `bun run typecheck` clean; PATCH bump to **0.56.5** in `herdr-plugin.toml`, `package.json`, `web/package.json` with a `CHANGELOG.md` `### Changed` line under a new `## [0.56.5]` heading: "Desktop mode: test coverage for Enter-sends and direct-typing arm/release".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; `web/src/lib/key-queue.ts`; any non-test file (product code is approved and does not change).
