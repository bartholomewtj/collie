Split `working` into thinking vs commands-running. Both halves this session: (1) a blue dot for commands-running, (2) hide the live tail while commands-only.

A tool part on the newest transcript entry with `result === undefined` means a command is running. That signal already exists in parseClaudeTranscript pendingTools (bridge/journal/claude.ts:139) and the same pairing in the codex / grok / opencode / pi adapters. No pane-text sniffing, no new regex, no Herdr change. AgentStatus on the wire stays idle|working|blocked|done|unknown — do NOT add a sixth status.

Change 1 — blue dot: boolean on AgentView (e.g. `runningCommand?: boolean`) that decorates `working`. StatusDot (web/src/components/status-badge.tsx) paints blue when the flag is set and status is working; amber ping stays for thinking. New `--status-*` token in web/src/index.css at the same lightness as the others so it does not out-shout blocked — read the solid-vs-hollow comment in status-badge.tsx. Dashboard needs the flag on the poll loop: journals are on-demand today (`/pane/:id/history`, bridge/server.ts:360). Bridge tails the journal per active agent pane each snapshot/poll and sets the flag; when `cfg.transcript` is off, omit the flag (today's amber). Do not change STATUS_RANK, session summary counts, notify prefs, or triage bucket keys.

Change 2 — hide live tail: add `commandsOnly` on LiveMirrorNeed (web/src/lib/transcript-seam.ts) and an explicit branch in liveMirrorNeeded. Do NOT let it fall through to `!newestTurnInViewport` — that latch requires a non-empty text part (newestTurnInViewport:174-181) and the commands-only case is often tool-parts-only, which would leave the tail up. `opts.pinned` still short-circuits everything (Show live stays the escape hatch). Call site already has `inline.entries` (web/src/components/agent-chat.tsx:302-311). Transcript ToolPart (web/src/components/transcript-view.tsx:60) already has name+summary: when `result` is missing, render a running row (`Bash(npm test) · running`) so hiding the tail is not a blank screen.

Where: web/src/lib/transcript-seam.ts (+ test), web/src/components/agent-chat.tsx (liveMirrorNeeded call), web/src/components/transcript-view.tsx (running tool row), web/src/components/status-badge.tsx (+ its tests if any), web/src/index.css, web/src/lib/types.ts AgentView, bridge/types.ts AgentView, bridge/state-engine.ts (snapshot assembly ~234), journal adapters under bridge/journal/, the poll/snapshot path that must attach the flag.

Done means: working+thinking is still amber; working+pending tool with no result is blue; live tail hides when commands-only unless Show live is pinned; a running tool row shows what is running; cfg.transcript off keeps amber; tests cover liveMirrorNeeded(commandsOnly) and the shipped runningCommand/dot logic (not a re-implementation). `cd web && bun run test` and `bun test ./bridge ./scripts` pass. MINOR bump 0.45.1 → 0.46.0 (new capability) in herdr-plugin.toml, package.json, web/package.json, CHANGELOG Added.

Out of scope: a sixth AgentStatus, Herdr API / HERDR_API.md, STATUS_RANK, notify prefs, triage classifier (lib/triage.ts), session summary working-count meaning, adws/, quality.py, permissions.py, other issues, docs PRs #84/#86.
