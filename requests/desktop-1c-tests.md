Close the three blocking findings the reviewer left on commit 730efcf (`adws/adw_data/sessions/191ef3e8/context_handoff/review.md`, "Blocking" section) by expanding three new test files only — production code is approved and does not change. (1) `web/src/components/agent-chat-desktop.test.tsx`: use a two-pane same-tab fixture (same `workspaceId` and `tabId`, so `PaneStrip` can render); with `on: false` assert Switch pane, Panes, Keys, "Type into terminal", and "Attach image" are present and clicking the handle opens the sheet; with `on: true` assert those five are absent and the composer textarea and Send are still present; call `__resetDesktop()` in `beforeEach`. (2) `web/src/hooks/use-desktop-hotkeys.test.tsx`: mount with `createMemoryRouter` and a pathname probe; assert Ctrl+Alt+Down from the first pane navigates to `panePath` of the next pane in `triage(agents)` order, Ctrl+Alt+Up goes to the previous, both wrap at the ends, plain ArrowDown / Ctrl+Alt+Left / Ctrl+Shift+Alt+Down are not `defaultPrevented`, an empty agent list is a no-op, and unmount removes the listener; wrap every dispatch in `act()`. (3) `web/src/routes/root-title.test.tsx`: add the case `on: true` with zero "needs" agents → `document.title === "Collie"`, and build a fresh `data` object per case instead of mutating a shared one.

Where: `web/src/components/agent-chat-desktop.test.tsx`, `web/src/hooks/use-desktop-hotkeys.test.tsx`, `web/src/routes/root-title.test.tsx`; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: all three files carry the assertions above and `cd web && bun run test` passes with every file that existed before 730efcf unedited; `bun run test` at root still reports 859 tests; `bun run typecheck` clean; PATCH bump to **0.56.2** in `herdr-plugin.toml`, `package.json`, `web/package.json` with a `CHANGELOG.md` `### Changed` line under a new `## [0.56.2]` heading: "Desktop mode phase 1: test coverage for hidden controls, hotkeys and tab title".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-direct-typing.ts`; `web/src/hooks/use-long-press.ts`; any non-test file (production code is approved and does not change).
