Finish desktop mode phase 1 per `specs/desktop-mode-spec.md` §2, §5, §6 and §9, building on the `useDesktop()` store and `DesktopShell` already on this branch (read the spec's "Decisions" table first — do not re-open those). When `useDesktop().on`: in the pane view (`web/src/routes/agent-chat.tsx` and what it renders) do not render the swipe-up handle, the pane-switcher sheet, the `touch-none` strip, `PaneStrip`, the attach-image button, or the Keys and Type controls in the Controls row (`nav-tray.tsx`); hide the `tapToFocus` pref row in Settings; `App.tsx` passes `on ? 2 h : 30 min` to `useIdleLock`; `RootLayout` sets `document.title` to `(n) Collie` when `n > 0` else `Collie`, where `n` = `agents.filter(a => bucketOf(a) === "needs").length`, and never touches the title when `off`; add `useDesktopHotkeys()` called once in `DesktopShell` — a single `window` `keydown` listener in capture phase that handles only Ctrl+Alt+Up / Ctrl+Alt+Down = previous / next pane, order = `triage(agents)` flattened ("needs you" first, tree order within a section), and never `preventDefault`s any other chord.

Where: `web/src/routes/agent-chat.tsx`, `web/src/components/nav-tray.tsx`, the settings route, `web/src/App.tsx`, `web/src/routes/root.tsx`, `web/src/components/desktop-shell.tsx`, new `web/src/hooks/use-desktop-hotkeys.ts` (+ test), new tests beside each touched component.

Done means:
- New tests with `on: false` assert the tree still contains `BottomNav`, the Keys and Type controls, the attach button and the swipe handle, and does not contain `DesktopShell`, the sidebar, the strip or the hotkey listener; `document.title` is untouched; `useIdleLock` receives 30 min. With `on: true` the inverse holds, `useIdleLock` receives 2 h, the title is `(2) Collie` for two "needs" agents, and Ctrl+Alt+Down from the first pane navigates to the next in triage order while a plain ArrowDown is not prevented.
- `bun run test` at root still reports 859 tests across 37 files and `cd web && bun run test` still reports every existing test passing with **no existing test file edited**; new tests only add to the web count. `bun run typecheck` is clean.
- PATCH bump to **0.56.1** in `herdr-plugin.toml`, `package.json`, `web/package.json`, with one `CHANGELOG.md` `### Added` line under a new `## [0.56.1]` heading: "phone-only controls hidden, 2 h idle pause, `(n) Collie` tab title, Ctrl+Alt+Up/Down pane switch".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-direct-typing.ts`, `web/src/components/composer.tsx`, `web/src/hooks/use-long-press.ts` (phases 2–3); Ctrl+` and Ctrl+F (phase 2); `setAppBadge`; any change to what renders when `on` is false.
