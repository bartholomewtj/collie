Add desktop mode's number-key prompt picking per `specs/desktop-mode-spec.md` §10 phase 4 (read the spec's "Decisions" table first — do not re-open those). When `useDesktop().on`, the composer surface is shown (not direct typing) and the pane's current block is a recognised `PromptModel` (`web/src/lib/harness/prompt-model.ts`): memoise the `buildBlocks` result in `web/src/components/agent-chat.tsx` (today only a `dialogPresent` boolean is derived), pass the current `promptBlock` to `Composer`, and a bare digit keydown in the empty composer box chooses `prompt.options.find(o => o.keys[0] === digit)` and routes it through the existing `handlePromptAction` / `submitPromptOption` dialog guard — never raw keys. A digit with no matching option, or any non-prompt dialog (plan text row, wizard, multi-select, free-text feedback focused), is a no-op that shows a status "No option N" / "Use the buttons for this dialog"; the digit does not reach the pane and is not typed into the box. With the box non-empty the digit types normally. With `on: false` nothing changes. While direct typing is armed digits still go to the pane raw (exists). Confirm History and Files already drop `max-w-screen-sm` in desktop mode (§7) and add a one-line "Number keys pick prompt options" note to the README "Desktop mode" section.

Where: `web/src/components/agent-chat.tsx`, `web/src/components/composer.tsx`, `web/src/lib/prompt-action.ts` only if it needs a lookup helper, `README.md`, new tests beside each touched file; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: tests cover a digit in the empty box with `on: true` calling the prompt action with the matching option (guard path, no `sendKeys` with a raw digit), a digit with no match showing the status and sending nothing, a plan dialog's text row never receiving a raw digit, a digit in a non-empty box typing normally, and `on: false` untouched. `scripts/check-doc-links.sh` passes. `cd web && bun run test` passes with no existing test file edited; root `bun run test` still 859; `bun run typecheck` clean; PATCH bump to **0.56.13** in `herdr-plugin.toml`, `package.json`, `web/package.json` with one `CHANGELOG.md` `### Added` line under `## [0.56.13]`: "Desktop mode: number keys pick prompt options through the dialog guard".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; `web/src/hooks/use-direct-typing.ts`; `web/src/lib/dialog-guard.ts`.
