Finish desktop mode phase 2 per `specs/desktop-mode-spec.md` §3 "Paste", §6 (the Ctrl+F row) and §10 phase 2 (read the spec's "Decisions" table first — do not re-open those). When `useDesktop().on`: (1) paste while captured — single-line text with no newline and not `isDestructiveInput` is sent via `textToKeySequence` as today; otherwise the text is held in the direct-typing strip with "Paste N lines into the terminal? Send · Discard"; Send keeps newlines as `Enter` keys and never appends a trailing Enter; image paste goes to `onPasteImage` and the returned host path shows in the strip as a one-click "Type path" chip, never typed unprompted. (2) Ctrl+F in `useDesktopHotkeys` opens the Collie find bar on the pane and history routes only when not captured; not intercepted on tree or settings; Esc / Enter / Shift+Enter behave as the find bar does today. (3) Docs: a "Desktop mode" section in `README.md` (how to turn it on, the two typing surfaces, the hotkeys, and "keys the browser keeps": Ctrl+T, Ctrl+W, Ctrl+N, Ctrl+Shift+N, Ctrl+Tab, Ctrl+Shift+T, Alt+Left/Right, F11, plus Ctrl+C with a selection, Ctrl+R, F5); update `NEXT-SESSION.md` to say phase 2 is done and phases 3–4 wait a week of use.

Where: `web/src/hooks/use-direct-typing.ts`, the file that defines `DirectTypingStrip` (verify its path), `web/src/hooks/use-desktop-hotkeys.ts`, `web/src/components/find-bar.tsx` only if it needs an open-from-outside prop, `README.md`, `NEXT-SESSION.md`, new tests beside each touched file; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: tests cover paste single-line, paste multi-line held then sent as `Enter` keys with no trailing Enter, paste `rm -rf` held, Discard, image-paste chip not auto-typed, Ctrl+F opens find only when not captured and not on tree/settings. `scripts/check-doc-links.sh` passes. `cd web && bun run test` passes with no existing test file edited; root still 859; typecheck clean; PATCH bump to **0.56.6** with one `CHANGELOG.md` `### Added` line under `## [0.56.6]`: "Desktop mode: paste hold, Ctrl+F find, README section".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-long-press.ts`; `web/src/components/composer.tsx`; drag-and-drop upload, right-click popover and number-key prompt picking (phases 3–4).
