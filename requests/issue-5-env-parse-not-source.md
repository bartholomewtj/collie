Fix issue #5: parse `.env` in `collie-ctl.sh` instead of sourcing it with `set -a; .`, scope secret exports to the bridge process and `push-test` only, and harden permissions (`chmod 700` config dir, `chmod 600` `.env`) on `start` with warnings when permissions were wider. Replace bare `bun` in `self_dnsname` with `"$BUN"`.
Where: scripts/collie-ctl.sh (lines 42, 83-97, 177, 333, 370, 376, 425-430, 438-442, 446-448, 945-948), scripts/collie-ctl.test.sh, .env.example, README.md.
Done means: `.env` is parsed line-by-line without eval/source; malformed lines warn with line numbers without echoing secrets; parsed variables are not exported globally; bridge launch and `push-test` export them in subshells/exec; permissions are tightened to 700/600 on start; docs reflect `chmod 600` and no-exec semantics; new lifecycle tests pass.
Out of scope: audit issues (#1-#4, #6-#12), changes to `contrib/windows/collie-ctl.ps1`, changing how systemd reads `EnvironmentFile`, `.env` schema validation, and passing `COLLIE_DEV_*` to builds spawned by `collie-ctl.sh`.
