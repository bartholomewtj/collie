Make desktop mode's layout fixed with exactly two scroll regions, per `specs/desktop-mode-spec.md` §2a (read the spec's "Decisions" table first — do not re-open those). When `useDesktop().on`: the page never scrolls (`body` / `#root` overflow hidden at 100 dvh); the off-ramp strip, the sidebar (its space tree scrolls inside its own box if it overflows), the pane `AppHeader`, the statusline strip, the composer / direct-typing strip and the prompt buttons are always on screen; the pane mirror is a `min-h-0 flex-1 overflow-y-auto` box between the fixed header and the fixed bottom controls, and the History route is the same shape with the transcript as the scrolling child. When `on` is false nothing changes.

Where: `web/src/components/desktop-shell.tsx`, `web/src/components/desktop-sidebar.tsx`, `web/src/components/agent-chat.tsx`, `web/src/routes/history.tsx`, `web/src/routes/root.tsx`, `web/src/index.css` (only if a desktop-gated rule is needed), new tests beside each touched file; `herdr-plugin.toml`, `package.json`, `web/package.json`, `CHANGELOG.md`.

Done means: with `on: true`, a pane with 500 mirror lines leaves the header, statusline and composer in the DOM with `overflow-y-auto` only on the mirror box (and the sidebar tree box), and History has `overflow-y-auto` only on the transcript container; with `on: false` the class lists on those elements are exactly what they are at `3ce34b7`. `cd web && bun run test` passes with no existing test file edited; `bun run test` at root still reports 859 tests; `bun run typecheck` clean; PATCH bump to **0.56.3** in `herdr-plugin.toml`, `package.json`, `web/package.json`, with one `CHANGELOG.md` `### Changed` line under a new `## [0.56.3]` heading: "Desktop mode: only the mirror and History scroll; sidebar, header and composer stay fixed".

Out of scope: `web/src/hooks/use-display-prefs.ts`; `bridge/`; `web/src/hooks/use-direct-typing.ts`; `web/src/hooks/use-long-press.ts`; `web/src/components/composer.tsx`; resizing the sidebar; any phone layout change.
